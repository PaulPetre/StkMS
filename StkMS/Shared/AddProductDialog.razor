@page "/AddProduct"

@using StkMS.Contracts
@using StkMS.Library.Contracts
@using StkMS.ViewModels
@inject IMapper mapper
@inject IApiClient apiClient
@inject IDialogService DialogService

<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.Add" Class="mr-3 mb-n1" />
            Adauga produs
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudTextField Value="@stockViewModel.Code" Label="Cod Produs" ReadOnly="true" />
        <MudTextField Value="@stockViewModel.Name" Label="Server Name" ReadOnly="true" />
        <MudTextField Value="@stockViewModel.Unit" Label="Location" ReadOnly="true" />
        <MudTextField Value="@stockViewModel.UnitPrice" Label="IP Address" ReadOnly="true" />
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Error" OnClick="AddOrUpdateProduct">Adauga</MudButton>
    </DialogActions>
</MudDialog>

@code
{
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }

    [Parameter]
    public StockViewModel stockViewModel { get; set; }

    void Cancel() => MudDialog.Cancel();
    private async void AddOrUpdateProduct()
    {
       
    }

}
